# Staging Model Guidance

- For files under `models/staging/`, pay extra attention to naming consistency, source alignment, and whether staging models stay thin.
- Flag transformations that look mart-like or business-logic-heavy if they appear in staging models.
